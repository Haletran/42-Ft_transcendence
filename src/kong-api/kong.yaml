_format_version: "3.0"
_transform: true

services:
- url: https://localhost:9000 # pour l'instant on redir tout
  name: django-credentials_service
  routes:
  - name: register_route
    paths:
    - /api # could be api/register
    strip_path: false
