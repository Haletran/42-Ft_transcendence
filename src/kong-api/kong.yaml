_format_version: "3.0"
_transform: true

services:
- name: django-credentials_service
  url: https://www.google.com/ # pour l'instant on redir tout
  routes:
  - name: django-credentials_route
    paths:
    - /api/register
    - /api/login
