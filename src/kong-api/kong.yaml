_format_version: "3.0"
_transform: true

services:
- name: django-credentials_service
  url: http://django-credentials:9000 # pour l'instant on redir tout

routes:
  - name: django-credentials_route
    service: django-credentials_service
    paths:
      - /api/register
      - /api/login
