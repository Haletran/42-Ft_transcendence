_format_version: "3.0"
_transform: true

services:
- url: https://localhost:9000
  name: django-credentials_service
  routes:
  - name: register_route
    paths:
    - /api/register
    strip_path: false
